version: '3.8'
services:
  bridge:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - BRIDGE_HOST=0.0.0.0
      - BRIDGE_POLITE_MODE=1
    volumes:
      - ./logs:/app/logs
